version: '3.8'

services:
  app:
    build: .
    command: uvicorn app.main:app --host 0.0.0.0 --reload
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    environment:
      - ENV_VAR_NAME=value
    networks:
      - app-network

networks:
  app-network:
    driver: bridge